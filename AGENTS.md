# CaveSky 协作说明

任何新任务开始工作前，依次阅读：

1. `docs/PROJECT_CONTEXT.md`
2. `docs/TASKS.md`
3. 与本次工作相关的 `docs/decisions/` 文档
4. 修改镜头格式时，再阅读 `docs/shot-project-spec-v0.1.md`

## 协作规则

- 开始修改前，在 `docs/TASKS.md` 登记负责人、状态和文件范围。
- 不要修改其他进行中任务占用的文件；发生重叠时先停止并说明冲突。
- 前端主要修改 `apps/editor/`，后端主要修改 `cavesky/`，模型适配器应保持独立边界。
- 不要把 VACE、SAM、RIFE 或任何供应商字段直接写进通用镜头格式。
- 不覆盖用户的未提交改动，不执行破坏性 Git 操作。
- 完成后运行与改动相称的检查，并更新 `docs/TASKS.md` 和“最近交接”。
- 重要且长期有效的架构决定写入 `docs/decisions/ADR-*.md`。

## 当前产品原则

创作者决定元素、关键状态和动作意图；AI 负责生成关键状态之间的自然过渡。优先保持作者控制、局部重生成、资产复用和模型可替换性。
